
if(coodX+1 == current->x && coodY+1 == current->y){
    printf("%c", current->content);
}else if (coodX+1 == current->next->x && coodY+1 == current->next->y){
    printf("%c", current->next->content);
}else{
    printf(" ");
}







/////////////////////:

pixel *current = pixels;
while(current != NULL){
    
    if(coodX+1 == current->x && coodY+1 == current->y){
        printf("%c", current->content);
    }else{
        printf(".");
    }
    c++;

    current = current->next;
}


/////////////////////////// testing
#include <stdio.h>
#include <unistd.h>
#include <sys/ioctl.h>
#include <stdlib.h>

typedef struct pixel{
    int x;
    int y;
    char content;
    struct pixel *next;
}pixel;

void frame(struct pixel *pixels);
void termnialSize(int *lines, int *col);

//printf("\e[?25l"); // disable cursor
//printf("\e[?25h");  //  enable cursor


int main(){
    printf("\e[?25l");
    

    
    struct pixel *head = NULL;

    head = (pixel *) malloc(sizeof(pixel));
    printf("pointer of arrow 1 -> %p\n", head);

    head->x = 10;
    head->y = 10;
    head->content = '-';

    head->next = (pixel *) malloc(sizeof(pixel));
    head->next->x = 11;
    head->next->y = 10;
    head->next->content = '>';
    printf("pointer of arrow 2 -> %p \n", head->next);

    head->next->next = NULL;

    printf("pointer of arrow 3 -> %p \n", head->next->next);

    frame(head);
    /*
    for(int x=0; x!=3; x++){
        //system("clear");

        frame(head);
        head->x++;
        head->next->x++;
        

        sleep(1);        
    }
    */
    
    printf("\e[?25h");
}

void frame(struct pixel *pixels){
    
    int coodX, coodY = 0;
    int col, lines;
    int c = 0; 

    termnialSize(&lines, &col);
    printf("///////////\n");

    


    
    for(int i=0; i!= lines; i++){
        for(int x=0; x!= col-1; x++){
            
            pixel *current = pixels;
    
                while(current != NULL){

                    if(current->x == coodX+1 && current->y == coodY+1){
                        printf("%c", current->content);
                    }else{
                        printf(".");
                    }
                    
                    current = current->next;

                }
               
                coodX++;
            }
            
            
        }
        printf(".\n");
        coodX=0;
        coodY++;

    fflush(stdout);
}

   
    



void termnialSize(int *lines, int *col){
    struct winsize w;
    ioctl(STDOUT_FILENO, TIOCGWINSZ, &w);

    *lines = (int) w.ws_row - 1;
    *col = (int) w.ws_col;
}



///////////////////////////////
WORKING
///////////////////////////////




#include <stdio.h>
#include <unistd.h>
#include <sys/ioctl.h>
#include <stdlib.h>

typedef struct pixel{
    int x;
    int y;
    char content;
    struct pixel *next;
}pixel;

void frame(struct pixel *pixels);
void termnialSize(int *lines, int *col);

//printf("\e[?25l"); // disable cursor
//printf("\e[?25h");  //  enable cursor


int main(){
    printf("\e[?25l");
    

    
    struct pixel *head = NULL;

    head = (pixel *) malloc(sizeof(pixel));

    head->x = 10;
    head->y = 10;
    head->content = '-';

    head->next = (pixel *) malloc(sizeof(pixel));
    head->next->x = 11;
    head->next->y = 10;
    head->next->content = '>';

    head->next->next = NULL;

    
    for(int x=0; x!=6; x++){
        system("clear");

        frame(head);
        head->x++;
        head->next->x++;
        

        sleep(1);        
    }
    
    printf("\e[?25h");
}

void frame(struct pixel *pixels){
    
    int coodX, coodY = 0;
    int col, lines;

    termnialSize(&lines, &col);
    pixel *current = pixels;

    for(int i=0; i!= lines; i++){
        for(int x=0; x!= col-1; x++){
            
            if(coodX+1 == current->x && coodY+1 == current->y){
                printf("%c", current->content);
            }else if (coodX+1 == current->next->x && coodY+1 == current->next->y){
                printf("%c", current->next->content);
            }else{
                printf(" ");
            }

            coodX++;
        }
        printf(" \n");
        coodX=0;
        coodY++;
    }

    fflush(stdout);
}

void termnialSize(int *lines, int *col){
    struct winsize w;
    ioctl(STDOUT_FILENO, TIOCGWINSZ, &w);

    *lines = (int) w.ws_row - 1;
    *col = (int) w.ws_col;
}


///////////////////////////
////////////////////////////
://///////////////////////

#include <stdio.h>
#include <unistd.h>
#include <sys/ioctl.h>
#include <stdlib.h>

typedef struct pixel{
    int x;
    int y;
    char content;
    struct pixel *next;
}pixel;

void frame(struct pixel *pixels);
void termnialSize(int *lines, int *col);

//printf("\e[?25l"); // disable cursor
//printf("\e[?25h");  //  enable cursor



void addPixel(int x, int y, char content, int numberInNode, struct pixel *head){
    
    int x =0;

    while(head != NULL){
        
        
        
        
        x++;    
    }
}



int main(){
    printf("\e[?25l");
    

    
    struct pixel *head = NULL;

    head = (pixel *) malloc(sizeof(pixel));

    head->x = 10;
    head->y = 10;
    head->content = '-';

    head->next = (pixel *) malloc(sizeof(pixel));
    head->next->x = 11;
    head->next->y = 10;
    head->next->content = '>';

    head->next->next = (pixel *) malloc(sizeof(pixel));
    head->next->next->x = 9;
    head->next->next->y = 10;
    head->next->next->content = '-';

    
    for(int x=0; x!=6; x++){
        system("clear");

        frame(head);
        head->x++;
        head->next->x++;
        head->next->next->x++;
        

        sleep(1);        
    }
    
    printf("\e[?25h");
}

void frame(struct pixel *pixels){
    
    int coodX, coodY = 0;
    int col, lines;
    int found = 0;

    termnialSize(&lines, &col);
    


    for(int i=0; i!= lines; i++){
        for(int x=0; x!= col-1; x++){

            pixel *current = pixels;
            while(current != NULL){

                if(current->x == coodX+1 && current->y == coodY+1){
                    printf("%c", current->content);
                    found = 1;
                }

                current = current->next;

            }
            if(found == 0 ){
                printf(" ");
            }

            
            coodX++;
        }
        printf(" \n");
        coodX=0;
        coodY++;
    }
    fflush(stdout);
}

void termnialSize(int *lines, int *col){
    struct winsize w;
    ioctl(STDOUT_FILENO, TIOCGWINSZ, &w);

    *lines = (int) w.ws_row - 1;
    *col = (int) w.ws_col;
}